# SQLMesh configuration
# Docs: https://sqlmesh.readthedocs.io/en/stable/reference/configuration/

gateways:
  local:
    connection:
      type: duckdb
      database: fitness.duckdb

default_gateway: local

model_defaults:
  dialect: duckdb
  start: "2024-01-01"

